# Turn all outputs (from details and pizzas) to input streams for the pizza-app-control

# Check out the animation code

# Add type for pizzas prices

# Make ContextService out of HttpClient

# Extract logic out of the view (as much as possible)
On-going

# Fix a bug on Place Order button
If the pizzas are selected before the user details are filled out then
the Place Order button might be disabled.
